name: CI - Exemplo Didático

on:
  push:
    branches:
      - main
    paths:
      - pipelines/ci-exemplo.yml
  pull_request:
    branches:
      - main
  workflow_dispatch:

permissions:
  contents: read

jobs:
  exemplo:
    name: Pipeline de Exemplo
    runs-on: ubuntu-latest

    steps:
      - name: Checkout do repositório
        uses: actions/checkout@v4

      - name: Executar etapa de demonstração
        run: |
          echo "Pipeline funcionando com sucesso!"
          echo "Este workflow é apenas um exemplo didático."
